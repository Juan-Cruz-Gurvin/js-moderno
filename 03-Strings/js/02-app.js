const producto = 'Monitor 20 Pulgadas';
console.log(producto);

// Conocer la cantidad de letras de la cadena de texto 
console.log(producto.length);


// Nos dice en que posicion empieza si es que tiene el string que pusimos (-1 si no se encuentra)
console.log(producto.indexOf('Monitor'));


// Nos dice con booleanos (true o false) si se encuentra el string que pusimos
console.log(producto.includes('Tablet'));
console.log(producto.includes('Monitor'));
console.log(producto.includes('monitor'));